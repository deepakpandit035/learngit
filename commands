git config -- global user.email "deepakpandit.035@gmail.com"
git config -- global user.name "Deepak Kumar"

Learn git part - 1:
1)  git init
2)  git add [filename] || -A
3)  git status
4)  git commit -m "msg"
5)  git remote and origin repoName
6)  git push origin master
7)  git clone repoName
8)  git pull origin  master
